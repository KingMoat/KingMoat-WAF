# Changelog

## Unreleased

### 新增

- **站点页全局监听设置入口**：站点管理页顶部新增「全局监听设置」，可直接配置数据面 HTTP / HTTPS 监听地址与全局 ACME 联系邮箱，随「发布配置」一并生效（此前监听配置在控制台无任何入口）。同时新增发布前置校验：监听地址格式非法（端口须 1-65535、HTTP 与 HTTPS 端口须不同）、站点启用 ACME 自动证书或 HTTPS 跳转但未配置全局 HTTPS 监听时，保存/发布会被前端拦截并引导到设置项，不再等到发布时才报服务端校验错误。

## v0.7.7-beta (2026-09-26)

基于 v0.7.6-beta，配置与数据库完全兼容、无需迁移。**已部署实例升级不受影响**：安装脚本升级路径保留现有监听配置与控制台端口，默认端口变更仅作用于全新安装。

### 变更

- **配置校验报错提示友好化**：站点启用 ACME 自动证书或 HTTP 跳转 HTTPS 但缺少前置条件（联系邮箱、全局 HTTPS 监听地址、证书）时，报错从配置字段术语改为可操作的指引文案——按站点域名标识站点，并指出需在哪个页面完成哪项设置
- **全新安装默认端口变更**：一键部署的默认端口调整为——数据面 HTTP `80` / HTTPS `443`（默认启用 HTTPS 数据面监听；此前数据面仅监听本机回环地址的单一高位端口、HTTPS 默认关闭），控制台 `8443`（此前为高位非标端口）。安装过程交互选择三个端口，并用 `ss` 检测端口是否已被宿主机上其他进程监听：被占用时交互模式要求重选，非交互安装（`-y`）遇占用或端口冲突直接报错退出，可用新增的 `--http-port` / `--https-port` / `--console-port` 参数指定端口；三个端口两两互斥。80/443 为特权端口，一键部署以 root + `CAP_NET_BIND_SERVICE` 运行可直接绑定。**已部署实例不受影响**：重跑安装脚本升级时保留现有 `config.json` 监听配置与控制台端口，不会重置为默认值；升级时显式传入的 `--http-port` / `--https-port` / `--console-port` 参数优先生效（此前会被旧配置/旧服务端口静默覆盖），未传参时才沿用旧值。交互模式回车语义明确化：全新安装数据面 HTTPS 直接回车即启用（默认 `0.0.0.0:443`，与 `-y` 模式及安装横幅一致），旧实例升级直接回车保持现状（HTTPS 原本关闭则保持关闭，提示语已明确标注）

### 修复

- **站点发布启用 ACME 后证书签发不生效，需重启服务**：ACME 管理器此前仅在进程启动时构建一次，控制台发布后新增、修改或删除 ACME 站点（自动证书）均不会生效——新增站点 443 握手一直报无证书，删除站点后原域名仍被 ACME 接管，直至重启服务。现随每次发布自动重建 ACME 管理器：新域名即时可签发/续签，删除域名即时停止响应；80 端口 HTTP-01 验证分发与 443 证书选择链同步动态化（首次启用 ACME 后无需重启即可通过验证）
- **一键安装管道模式可能执行残缺副本**：`curl | bash` 时 bash 对管道脚本分块预读，安装脚本内构造临时副本时只能读到未缓冲的尾部（缺函数定义），真实安装中出现 `need_root`/`log` 等未定义错误。副本现改为从固定源地址重新下载完整脚本（与 bash 预读解耦，双源容错），保留下载后语法校验与临时副本清理。
- **关闭 HTTP/2 或自定义 TLS 套件档位的站点丢失 ACME 证书兜底**：站点配置 `http2_enabled=false` 或 `tls_profile` 覆盖时，443 握手改用站点级 TLS 覆盖配置，而该配置此前只携带站点证书路由、未接入监听级「站点静态证书优先、ACME 兜底」选择链——仅依赖 ACME 自动证书（未配置静态证书）的此类站点 443 握手永远失败、证书无法签发（默认配置站点不受影响）。现站点级覆盖配置统一重新挂载监听级证书选择链，与默认站点行为一致

## v0.7.6-beta (2026-09-26)

基于 v0.7.5-beta，配置与数据库完全兼容、无需迁移。**紧急修复版：v0.7.5-beta 的 Linux 包在 systemd 严格沙箱（如 openEuler 默认加固）下服务无法启动，受影响用户请升级**。

### 修复

- **一键安装在 systemd 严格沙箱下启动失败**：openEuler 等发行版的 systemd 单元（ProtectSystem=strict 且 /tmp 不在读写白名单）下，coraza 引擎构建时的 /tmp 可写性探测失败导致服务冷启动退出（`filesystem access check: ... read-only file system`）。修复：构建改用 coraza 的 `no_fs_access` 构建标签——部署形态全部检测资源内嵌，本就不需要引擎侧本地文件访问，探测直接跳过；同时收窄了引擎的文件系统攻击面
- **一键部署脚本在真实 Linux 上提取失败**：`curl | bash` 安装在解压阶段连续报 `mv: ... are the same file` 并以 `kingmoat binary not found in archive` 终止——临时目录名（kingmoat-install.*）被包裹目录查找模式 `kingmoat*` 误匹配，归位步骤把目录内容移到自身（无效操作）；查找现限定解压根下一层且不会匹配临时目录自身。已在同构包裹/平铺两种包布局下回归验证。
- **一键部署脚本非交互调用不再被误拦**：`bash install.sh -y < /dev/null`、CI 或远端 ssh 等无终端场景此前会被管道安装守卫拦截报错，现在参数解析前移、守卫仅对需要交互的管道安装生效；重执行时完整透传原始参数（如 `--version`/`--data-dir`）
- **一键部署输入校验加固**：安装/数据目录含双引号、`$`、反引号直接拒绝；控制台端口校验修复前导零（如 `08`）绕过问题并限制长度；数据目录文件系统检测改用 findmnt 精确识别（无 findmnt 时回退 df -T），不再依赖 df 输出字符串巧合
- **一键部署脚本可移植性小修**：临时副本 mktemp 模板去掉 `.sh` 后缀（部分精简系统的 mktemp 不支持带后缀模板）；伪造 `KINGMOAT_REEXEC` 环境变量不再能绕过管道守卫或被误删；非 root 与空输入报错提示改为可照做的文档化调用方式
- **一键部署升级失败不再让服务停摆**：升级中途（替换二进制、重载配置等）任何失败，退出前自动尝试重新拉起服务（尽力而为，不做版本回滚）
- **一键部署清理与信号处理统一**：临时目录与管道安装临时副本的清理合并为单一退出钩子，下载/安装中途被终止（kill/超时）也不再残留 /tmp
- **一键部署下载 fallback 补全**：Gitee 不可达时校验和文件改与二进制同源从 GitHub 获取（双源尝试），Gitee 单点故障不再拖垮整体安装；校验失败即拒绝安装的安全语义不变
- **一键部署卸载安全加固**：不再盲目执行安装时持久化的配置文件，改为仅识别安装/数据目录两个已知键并校验合法性（非法值跳过回落默认）；命令行 `--data-dir` 不再被持久化配置覆盖
- **一键部署管道安装残缺副本拦截**：下载脚本中途断流产生的残缺副本在重执行前被语法校验拦截（大幅收窄部分执行窗口）

### 工程

- CI 新增 shellcheck 步骤（扫描 deploy/install.sh 与 scripts/build.sh，部署脚本静态检查进门禁）；工作流权限收敛为最小化（默认只读，仅发布步骤保留写权限）
- 构建入口统一携带 coraza `no_fs_access` 标签（构建脚本、Dockerfile、CI 冒烟与测试全链路对齐），发布产物与开发/容器变体行为一致

## v0.7.5-beta (2026-09-25)

基于 v0.7.4-beta，配置与数据库完全兼容、无需迁移。
### 新增

- **防护总览时间范围对齐**：新增每日每结果请求历史持久化（35 天保留，随数据目录存储、重启可恢复；跨午夜后首次持久化先将未落盘计数归昨日日期，不再错日）；「请求数」「拦截率」两张卡从「安装以来累计」改为「所选时间范围内汇总」（今日/近7日/近30日切换联动）；攻击来源 Geo/IP TOP5 改为跟随主范围选择器（支持 `?days=N` 日历对齐查询，保留 `hours=` 回退）；卡片文案与口径对齐（去「累计/今日」过期标签，拦截卡标注「走势·近48h」）；日计数器新增测试重置钩子消除跨测试互扰
- **站点编辑抽屉补「固定回源 SNI」（sni_host）输入框**：后端配置与校验已支持但控制台此前无入口——仅当任一上游节点为 HTTPS 协议时显示，与「SNI 转发」互斥（前端同步提示 + 提交拦截，后端校验兜底拒绝）；同步补齐 sni_host 配置校验测试（合法配置通过 / 与 SNI 转发互斥拒绝 / 非法主机名拒绝）

### 变更

- **微引擎命中日志统一由「记录日志」开关管控**：关闭检测类（disable）规则的命中记录不再强制写入防护日志（撤销 v0.7.3-beta 引入的强制审计），与放行/观察类一致由规则的「记录日志」开关控制。升级提示：存量关闭检测类规则若开关为关，升级后命中不再记日志；需要追溯命中记录请手动打开该规则开关
- **规则编辑默认值联动**：微引擎规则弹窗切换匹配动作时「记录日志」按默认值联动——关闭检测类默认开启，其余（拦截/放行/仅观察）默认关闭，可手动调整
- **请求历史启动恢复**：启动时从数据目录恢复每日每结果请求历史（35 天保留），重启后「近7日/近30日」视图数据不丢（此前仅恢复当日计数）

### 修复

- **站点编辑抽屉清空「固定回源 SNI」后保存即移除该配置**：保存路径此前仅在填写时正向写入 `sni_host`，清空输入不显式移除旧值；现清空保存会真正删除该字段，不再残留旧配置与「SNI 转发」互斥校验冲突且难以排查
- **微引擎规则名查重与保存口径统一**：前端查重用去空白后的名字、落库却保存原始输入（如带尾随空格的空白变体可绕过查重，并使按名定位/命中计数错位）；现查重与落库统一用去空白后的名字（与服务端校验同口径），查重基准同时改为发布前刚拉取的服务端配置，并发新增的同名规则也会被拦下
- **防护总览迷你走势线与「近48h」标注口径一致**：拦截卡迷你走势线此前只渲染最近 14 个小时桶，与「走势·近48h」标注不符；现铺满全部 48 个小时桶
- **stats 端点请求历史快照开销消除**：`/api/stats` 不再把已是目标类型的每日请求历史快照做 JSON 序列化/反序列化往返（热端点纯开销），改为直接传入类型化快照
- **请求历史跨午夜竞态**：每日请求历史的折叠与跨日滚动此前分别加锁，跨日瞬间的一次持久化可能读到已被滚动的空快照并覆盖前一天的历史（整天计数清零且随状态文件持久化、不自愈），请求侧跨日滚动也可能丢掉最近一个持久化周期的计数；现折叠与滚动统一由同一把日锁贯穿，滚动前先把终值折入前一天历史（覆盖写幂等）。升级后历史数据不受影响
- **一键部署 `curl | bash` 管道安装修复**：管道安装自动把脚本转入临时副本重执行（环境变量标记防重入、守卫内报错内联输出、临时副本退出时清理，含提前失败路径，不再残留 /tmp）；重执行后若标准输入仍是已耗尽的管道或无终端可用（CI、嵌套自动化），交互提示此前会因 EOF 静默中断安装（无任何报错输出），现在自动回落脚本既有默认值继续安装（安装目录 /opt/kingmoat、数据目录 /var/lib/kingmoat、控制台端口 28443），重执行前还会尽力把标准输入恢复为终端以支持真实交互输入；`-y` 非交互模式行为不变
- **一键部署脚本加固**：`curl | bash` 管道安装时自动重执行脚本（交互式 read 不再吞掉后续脚本行）；校验和校验改为 fail-closed（checksums.txt 缺失或未命中即拒绝安装，并锚定匹配防文件名前缀碰撞）；安装目录/数据目录含空格或位于 /home、/root 下直接拒绝（systemd ProtectHome 沙箱不可见）；升级路径先停服务再替换二进制（防 ETXTBSY 中途失败）；控制台端口校验收紧为 1-65535；命令行参数缺值提前报错；卸载改为读取持久化安装位置（/etc/kingmoat-install.conf，安装时写入）；自定义安装/数据目录写入 systemd ReadWritePaths
- **站点「今日请求数」跨午夜计数错日**：持久化 flush 现在先滚动日计数再快照——本地午夜后首个 flush 周期不再把昨天的计数盖今日日期戳（此前重启后会被恢复为「今日」）；站点统计合并测试的事件时间戳改为当日已过时段中点（午夜前后跑测试不再偶发错日）
- **未匹配站点/停用站点的每日计数不再以请求 Host 作为标签**：请求 Host 由客户端可控，作为「今日请求数」的站点标签会形成无上界的每日标签集合（浪费内存且稀释统计）——改用固定空标签（API 侧本就过滤空标签，不展示）
- **站点编辑抽屉保存时保留站点级 CRS 分类覆盖**：分类覆盖不由编辑抽屉管理，编辑已有站点保存时回带原 `waf.categories`（此前被静默重置回「跟随全局默认」）；CRS 分类开关对非 operator 角色禁用（策略页与站点页）；微引擎规则名保存时前端查重（与后端校验互补，拦截改名冲突与存量重名）

## v0.7.4-beta (2026-09-24)

### 修复

- **RESPONSE-959 出站阻断评估从未加载**：内嵌 coreruleset 的 `RESPONSE-959-BLOCKING-EVALUATION.conf`（响应体敏感数据/WebShell 阻断打分）不在 always-on 清单也不属于任何检测分类，构建 WAF 时永不加载——分类过滤与默认全开形态下均缺失。修复：补入 always-on 清单；新增文件覆盖守卫测试 `TestAllCRSFilesAccountedFor`（内嵌每个 .conf 必须被 always-on 或分类清单引用，防静默丢弃再犯）

## v0.7.3-beta (2026-09-24)

### 新增

- **微引擎 disable 规则支持 CRS 分类粒度**（`coraza:<分类>`，如 `coraza:sqli`）：命中条件后按站点只关闭指定 CRS 检测分类、其余分类照常（如「路径前缀 /api/* → 关闭 sqli，XSS/RCE 保留」）。实现为发布时预编译变体引擎池（每规则分类集 + 并集；coraza 实例编译后规则集固定，无法按请求裁剪），命中请求当次即切换变体，站点级持续生效至下次发布（发布重置，与模块级 disable 语义一致）；REQUEST-999 悬空指令过滤对变体同样适用；无分类 disable 规则时零回归。同一规则内禁止 `coraza` 与 `coraza:<分类>` 混用（拆成两条规则）
- **新增 `GET /api/policy/disable-state` 只读端点**：返回当前各站点被禁用的检测模块/分类快照（auditor 及以上可读），控制台排障「为什么 SQLi 没拦」有据可查；发布响应新增 `apply` 状态字段（已保存但引擎加载失败时前端醒目提示）
- **策略页新增「检测分类与引擎」tab（位于「引擎与规则版本」上方）**：「CRS 检测分类全局默认」与「检测引擎启用情况（按站点聚合）」合并为同页双栏——左侧分类默认开关（新增「全部启用」快捷、关闭项弱化显示），右侧引擎聚合改为双列卡片网格（名称 + 启用计数徽标 + 覆盖率进度条，未启用引擎弱化）；微引擎 tab 内的原分类默认卡移除（防重复入口）

### 变更

- **发布校验前移**：per-site 分类 disable 规则上限（≤4）与全局变体总量上限（≤64）由引擎加载阶段前移到配置校验层拒绝（修订不落库），不再出现「发布显示成功、引擎加载失败停留旧状态」的静默降级；变体编译失败在冷启动时降级（放弃该变体回退主引擎 + Error 告警），仅主引擎失败才启动失败
- **disable 动作强制审计**：命中 disable 规则不再受「记录日志」开关门控（关闭检测模块是安全态势变更事件），审计携带 `matcher/<名称>` 与被禁用的模块/分类全文
- **微引擎规则名称唯一性校验**：配置校验层拒绝重名的微引擎规则（`policy: matchers[N]: duplicate rule name "..."`）——控制台按名称定位规则（开关/编辑/删除）、审计日志按 `matcher/<名称>` 标记命中，重名会使这些操作有歧义；历史重名配置在下次发布时需先改名
- **微引擎弹窗「关闭检测模块」表单重设计**：模块勾选改为带边框复选卡两行排布（替代原始多选）；「按分类关闭 CRS」改为可展开子面板（切换条 + 折叠面板 + 两列分类网格，带互斥提示），替代原先 10 个分类平铺堆在「CRS 签名检测」下方的布局；整模块与分类子项双向互斥联动（勾任一方自动清空另一方并提示，整模块勾选时子面板禁开），替代原先仅靠校验报错的体验
- **登录页产品特色列表更新**：融入一键部署、修正「双引擎」为「签名与语义双检测」的准确表述、第 3 条从模块罗列改为突出防护粒度（CRS 分类独立启停 + 微引擎条件规则）
- **文档同步**：CONFIG.md 补 `matchers.disable_stages` 的 `coraza:<分类>` 复合值/botchallenge/上限/强制审计/重名约束、策略级 `waf_categories` 与站点级 `waf.categories` 字段说明；API.md 补发布响应 `apply` 字段与 `GET /api/policy/disable-state` 端点；ARCHITECTURE.md §3.3 补分类变体引擎设计决策并修正代码路径引用（waf.go → stage.go）

### 修复

- **微引擎弹窗「关闭模块」漏列 BOT 挑战（botchallenge）**：后端 DisableableStages 已支持该模块，前端选项列表此前缺失，导致无法通过界面按模块关闭 BOT 挑战

### 测试

- **分类过滤编译级守卫参数化**：10 个检测类别逐一排除、各自单独编译验证（TestBuildWAFEachCategoryExcludedCompiles），覆盖 REQUEST-999 悬空指令修复的全部类别组合

## v0.7.2-rc3 (2026-09-24)

### 修复

- **分类过滤实际不生效**：CRS 常驻文件 REQUEST-999 内含约 50 条指向检测类别规则（930/932/941/942 文件）的 `SecRuleUpdateTargetById` 指令——排除任一被引用类别后 coraza 编译失败（`rule "932240" not found`）：冷启动直接退出，热重载 fail-static 静默保留旧引擎（控制台显示发布成功，分类过滤无任何效果）。修复：按 3 位文件号前缀识别被排除类别的悬空指令——含悬空指令的 CRS 文件从内嵌规则集读取、过滤后内联替换其 Include（无悬空指令的文件保持原样），指令串本身也过滤（覆盖用户自定义规则引用被排除规则的场景）。附编译级回归测试（逐类别排除/全关/默认均编译通过）与单测

## v0.7.2-rc2 (2026-09-23)

v0.7.0-rc1 之后的第二个候选版本：检测粒度与可观测性增强（CRS 分类过滤、微引擎命中计数与审计开关、站点级统计徽标），部署与运维改进（一键部署脚本、可选匿名安装统计、固定回源 SNI）。

### 新增

- **微引擎规则命中计数与审计开关**：每条微引擎规则（MatcherRule）运行时记录命中次数（进程内原子计数，重启归零，配置热重载不丢），新端点 `GET /api/policy/micro-rules/hits` 按当前配置对齐返回；规则新增 `log_enabled` 审计开关（默认关）——开启后命中该规则的请求写入审计日志（deny 拦截事件照常，allow/monitor/disable 命中新增 monitor 事件，均携带 `matcher/<名称>` 规则标识），关闭时仅计数不写审计、动作照常执行；Policy.vue 微引擎规则列表新增「命中次数」列与「记录日志」开关（编辑弹窗同步），命中且开启日志时可一键跳转攻击日志页按规则过滤
- **检测引擎分类过滤（CRS Categories per Site）**：WAF 检测引擎按攻击类型拆分为可独立启停的模块（SQLi / XSS / RCE / LFI / RFI / PHP / 通用注入 / 会话固定 / Java 反序列化 / 扫描器检测），每个站点可通过 `waf.categories` 配置启用的检测类别。缺省不设置 = 全部启用（向后兼容）。关闭的类别规则**不加载**（零性能损耗）。基础设施规则（协议校验/阻塞评分/响应分析）始终加载。Policy.vue 微引擎 tab 新增 CRS 检测分类全局默认卡片。控制台可通过 API 配置
- **站点列表统计徽标**：每站点显示当日请求数与攻击数（30s 自动刷新，攻击数红色高亮）
- **一键部署脚本**（`deploy/install.sh`）：支持 Debian 12+ / Ubuntu 24.04+ / openEuler 22.03+，自动安装依赖、从 Gitee（备用 GitHub）下载最新 Release、交互式选安装/数据目录、生成 systemd 守护并自启动；支持 `--version` 锁版本、`--data-dir` 非交互、`--uninstall` 卸载；deploy/README.md 同步新增一键部署入口，Releases 链接从 GitHub 修正为 Gitee
- **匿名安装统计（默认关闭，可一键关闭）**：内置可选的安装量统计——默认关闭，在「系统设置 → 通用设置 → 匿名安装统计」显式开启后生效。仅上报随机安装 ID、软件版本、操作系统架构、CPU 核数与安装方式；不采集主机名、用户名、内网 IP 或任何业务数据；支持行业标准 `DO_NOT_TRACK` 环境变量一键关闭（优先级最高，零请求零落盘）；连续 3 次无法连接统计服务自动永久停止上报（设置开关 关→开 可重新尝试）；配置键 `telemetry.enabled`；关于页新增发布者/技术支持联系方式；README 新增遥测声明与设置说明
- **upstream `sni_host`（固定回源 SNI）**：HTTPS 上游可配置固定回源 SNI——回源 TLS 握手的 ServerName 使用该域名（连接级，连接复用安全），与 `sni_forward` 互斥；配置校验拒绝非法主机名与两者同设

### 修复

- **攻击类型显示"综合评分"**：CRS 阻断评分规则（949110）触发时，审计日志的攻击类型从"综合评分"改为实际攻击类别（如 SQL 注入 / XSS）——通过 `primaryAttackRuleID()` 从 matched 规则中按 `attack-*` 标签优先归因
- **`sni_forward` 连接复用场景 SNI 错配**：SNI 转发转发的是 TCP 连接的 SNI 而非请求实际域名——keep-alive 连接被不同域名请求复用时，上游收到错配的 SNI/证书；需要固定回源身份的站点请改用 `sni_host`
- **站点级 `waf.categories` 校验缺失**：站点配置中拼错的检测类别名原先被静默忽略（等于关闭未识别类别、削弱防护），现发布时校验并拒绝非法值，与 policy 级 `waf_categories` 行为一致
- **微引擎编辑弹窗状态残留**：编辑弹窗先铺默认值再覆盖规则字段，前一条规则的「记录日志」等开关不再残留到下一条规则
- **微引擎规则并发写错位**：微引擎规则的开关切换、编辑保存、删除全部改为按规则名定位（原先按列表下标），其他管理员并发增删规则后不再写错条目；目标规则已被删除/改名时明确报错提示刷新
- **热重载遥测空指针防御**：热重载 goroutine 对 telemetry 回调增加判空与加锁读取（console 未启用或构建时序异常下不再有理论崩溃与数据竞争窗口）

### 变更

- **移除微引擎 tab「规则命中 TOP」区域**：Policy.vue 微引擎规则防护页不再展示 CRS 规则命中 TOP 排行（原调 `/api/stats/rules`，端点保留但控制台不再使用）；每条微引擎规则自身的命中数改由列表「命中次数」列呈现（见新增条目）
- **微引擎规则默认不写审计日志**：未开启 `log_enabled` 的微引擎规则命中时不再写审计事件（此前 deny 命中始终写审计）；需要日志的规则在控制台打开「记录日志」开关
- **Sites.vue 站点列表重新排版**：左列加宽至 340–380px，安全特性标签两行紧凑布局，统计徽标独立行展示
- **AI 数据源装配收拢为共享构建函数**（ai.NewCenterSources）：aiSources 由装配点内手写字面量改为统一调用，新增 DataSources 字段只改一处即可同步，配反射对称性测试防字段漏接

## v0.7.0-rc1 (2026-09-21)

KingMoat WAF 社区版首个公开候选版本（木兰宽松许可证 Mulan PSL v2）。

- 纯 Go 实现的 All-in-one WAF：自研 L7 反向代理 + Coraza（ModSecurity SecLang 兼容引擎）+ OWASP CRS 4.x 规则内嵌，libinjection 语义层第二道防线
- 不限制站点数与并发；单二进制部署，配置默认存 SQLite（revision 版本化、发布即热生效、一键回滚）
- Web 控制台（Vue 3）：站点防护图形化配置、攻击日志与一键加白、证书管理（Let's Encrypt ACME 自动续签）、API 资产梳理、风险中心、RBAC 三级（admin/operator/auditor）+ MFA + API Key
- AI 安全助手（只读）：SSE 流式对话、日志一键问 AI、定时日报与突增主动报告、出站动态脱敏、OpenAI 兼容多模板、可选 MCP 端点
- 风险引擎 R1–R7（observe-only：敏感暴露/未授权线索/登录爆破/影子 API/僵尸 API/管理面暴露/明文敏感参数）
- 数据面防护：CC 限流、滑块验证码与 JS 质询、IP 黑白名单与订阅式 IP 组、GeoIP、整站 Basic 认证、响应敏感信息脱敏、HTML 动态防护、拦截/观察双模式、拦截页定制（安全设置）
- 可观测与外发：Prometheus `/metrics`、攻击日志外发（Elasticsearch / Loki / ClickHouse / Kafka / S3 / Syslog）、访问日志外发、webhook / 邮件告警
- 部署形态：all-in-one（推荐）/ 静态配置文件；提供 Docker、systemd、Windows 服务化方案
- 如需多节点集中管理（节点管理）、界面品牌个性化定制或其他功能性定制，请联系作者

### 修复与优化

- **【修复】防护总览导出报表不全**：PNG / PDF 打印原来只输出顶部统计卡行；改为整页捕获（图表重绘、打印分页优化），HTML 报表补「攻击趋势」与「最新攻击」两节
- **【修复】上游健康检查与转发 TLS 策略不一致**：探针曾强制校验上游证书，自签 / 内部 CA 的 HTTPS 上游流量正常却持续被标记不健康；探针现遵循站点 `verify_tls` 策略，与转发路径一致
- **【修复】AI 助手对话 404**：`base_url` 形态自动规范化（无路径补 `/v1/chat/completions`，`/v1` 结尾拼 `/chat/completions`，已完整或自定义路径原样/尊重输入）；`api_key_env` 误填密钥字面量可自动识别使用并告警；`/api/ai/config` 的 `enabled` 语义改为「有效可用性」；AI 抽屉每次打开拉取最新状态，模型热重建有回归测试
- **【安全】拦截页存储型 XSS 校验加固**：通用 `on*=` 事件属性正则 + 实体解码复查 + 拒绝 `vbscript:` / `data:text/html`；拦截响应新增 script-free `Content-Security-Policy` 兜底
- **【安全】节点令牌最小权限**：节点身份从 operator 降为只读 auditor，令牌泄露不再能发布 / 回滚配置；合并重复的令牌校验段
- **【修复】磁盘守护二级回收失效**：`OldestEventTime` 查询了不存在的列导致 FIFO 兜底永不执行
- **【优化】风险中心**：明细表新增站点列与发生次数列；行点击打开详情抽屉（完整字段 + 证据明细键值表）；支持从详情一键跳转该站点攻击日志（`/logs?site=`）
- **【合规】DB-IP 署名补齐**：NOTICE / THIRD-PARTY-LICENSES / gen-licenses 增补内嵌 DB-IP IP to Country Lite（CC BY 4.0）署名
- **【文档】**：新增部署总览 `deploy/README.md`；修正 kingmoat.service 可写路径、docker-compose 非 root 端口、windows.md 控制台 HTTPS 与审计日志形态；API/AI/API-ASSETS/windows 去除早期版本标记；架构文档技术栈与基线信息与实现对齐
